[build]
builder = "dockerfile"
dockerfilePath = "hermes-agent/Dockerfile"

[deploy]
startCommand = "/workspace/start.sh"
healthcheckPath = "/v1/models"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[volumes]]
mountPath = "/opt/data"
name = "hermes-state"
