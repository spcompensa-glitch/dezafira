# ============================================================================
# DEZAFIRA — Dependências Python
# ============================================================================

# ─── Web ──────────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn==0.28.0
pydantic==2.6.4
httpx==0.27.0
python-dotenv==1.0.1

# ─── Database ─────────────────────────────────────────────────────────
SQLAlchemy==2.0.28

# ─── HTTP ─────────────────────────────────────────────────────────────
requests==2.31.0

# ─── Upload YouTube ───────────────────────────────────────────────────
playwright==1.42.0
google-auth==2.28.1
google-auth-oauthlib==1.2.0
google-api-python-client==2.122.0

# ─── Telegram Bot ─────────────────────────────────────────────────────
pyTelegramBotAPI==4.18.0

# ─── Trend Hunting ────────────────────────────────────────────────────
scrapling>=0.3.0

# ─── TTS — Kokoro (Apache 2.0, CPU-friendly) ─────────────────────────
kokoro>=0.9.4
soundfile>=0.12.0
pydub>=0.25.1

# ─── Subtitles — Whisper (MIT) ────────────────────────────────────────
whisper-timestamped>=1.14.4

# ─── Video — MoviePy (MIT, Fallback) ─────────────────────────────────
moviepy>=2.0.0

# ─── OpenMontage ─────────────────────────────────────────────────────
# Instalado como pacote editável: pip install -e SniperVideoEngine/OpenMontage
# Dependências do OpenMontage (incluídas no setup.py):
# pyyaml>=6.0, pydantic>=2.0, jsonschema>=4.20, Pillow>=10.0, numpy>=1.24

# ─── Fallback TTS removido — Kokoro é o motor único ────────────────────
